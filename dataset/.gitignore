# Do not commit the raw, uncompressed dataset objects in any case
ds_test/
ds_train/
ds_valid/

# Until we obtain permission to release the dataset, do not commit the compressed dataset objects.
ds_test.tar.xz
ds_train.tar.xz
ds_valid.tar.xz